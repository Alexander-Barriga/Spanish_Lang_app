/**
 * Centralised AsyncStorage keys.
 *
 * Bump the suffix (e.g. `_v1` -> `_v2`) when the meaning of the value changes
 * so older installs don't read stale data.
 */
export const ONBOARDING_PAYWALL_SHOWN_KEY = 'paywall_shown_onboarding_v1';
